import 'package:flutter/material.dart';

class AddDeletePage extends StatelessWidget {
  const AddDeletePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Hello from Add Delete Page'),
    );
  }
}